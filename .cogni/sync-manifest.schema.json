{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/cogni-dao/cogni/.cogni/sync-manifest.schema.json",
  "title": "Cogni multi-repo sync manifest (inverse form v2)",
  "description": "spec.repo-sync-contract — inverse manifest: declares global excludes + per-artifact divergences (omit_from_artifact + artifact_only). Anything else under hub is expected to mirror 1:1 across all artifacts. Mirrors the CATALOG_IS_SSOT pattern (infra/catalog/_schema.json).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "hub", "artifacts", "exclude", "divergences"],
  "properties": {
    "foundation": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 },
      "description": "FOUNDATION (Tier 1 + Tier 2) allowlist — the platform substrate node-template owns and syncs into every fork: flight contract (force-overwritten) + foundational app/runtime/library substrate (three-way merged). The ENUMERATED SSOT for the node-template→fork sync; Tier 3 (node identity/presentation/product) is the COMPLEMENT (everything NOT listed), sovereign-by-default, restored to the fork inside the merge — never overwritten (spec.repo-sync-contract FOUNDATION_IS_ENUMERATED / TIER3_IS_THE_COMPLEMENT). Polarity is chosen by failure-mode asymmetry: missing a foundation path is non-destructive (staleness), enumerating Tier 3 would let a missed entry silently clobber sovereign code. Read at runtime by the operator fork-sync from node-template's copy of this manifest. Distinct axis from divergences[] (hub↔artifact mirror drift)."
    },
    "schema": {
      "const": 2,
      "description": "Manifest schema version. Bump = lock-step PRs across all repos per MANIFEST_BOOTSTRAP."
    },
    "hub": {
      "type": "string",
      "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$",
      "description": "GitHub owner/repo of the canonical hub. Per HUB_IS_COGNI_MONOREPO this is cogni-dao/cogni."
    },
    "artifacts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["repo", "visibility"],
        "properties": {
          "repo": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$"
          },
          "visibility": {
            "enum": ["public", "private"],
            "description": "public = anonymous clone OK in detector; private = needs PAT (detector skips in v0.1)."
          }
        }
      }
    },
    "exclude": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 },
      "description": "Global glob excludes — never propagated to or from any artifact. Junk/caches/build outputs."
    },
    "divergences": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["artifact", "reason"],
        "properties": {
          "artifact": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$"
          },
          "reason": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable justification per DECLARED_DIVERGENCE."
          },
          "omit_from_artifact": {
            "type": "array",
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Globs of paths the hub has but this artifact MUST NOT carry."
          },
          "artifact_only": {
            "type": "array",
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 },
            "description": "Globs of paths the artifact has but the hub MUST NOT carry."
          }
        }
      }
    }
  }
}
